html
    head
        meta(charset="utf-8")
        title Swap and Read
        script(src="/socket.io/socket.io.js")
        link(
            rel="stylesheet" 
            href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" 
            integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" 
            crossorigin="anonymous"
        )
        link(
            rel="stylesheet" 
            href="../styles/stylesheet.css"
        )
        link(
            href="https://fonts.googleapis.com/css?family=Megrim|Open+Sans"
            rel="stylesheet"
        )
    body
        navigation
            nav(id="top-navbar" class="navbar navbar-inverse navbar-fixed-top")
                div(id="navbar-container" class="container-fluid")
                    a(href="/") Main
                    if user
                        //- -var profileLink = "/user/" + user.username
                        a(href="/profile" target="_self") Profile
                        a(href="/requests" target="_self") Requests
                        a(id="logout" href="/logout" target="_self") Log Out
            block content